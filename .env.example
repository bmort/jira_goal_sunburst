# Jira connection
JIRA_BASE_URL=https://jira.example.com
JIRA_TOKEN=
JIRA_REJECT_UNAUTHORIZED=true

# Application
PORT=8080
APP_ORIGIN=http://localhost:5173

# Caching
# TTL for cached Sunburst responses in milliseconds (default 30 minutes)
# Adjust to control how frequently fresh data is fetched from Jira
SUNBURST_CACHE_TTL_MS=1800000
